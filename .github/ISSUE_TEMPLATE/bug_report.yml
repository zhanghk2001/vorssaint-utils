name: "🐛 Bug Report"
description: Report a bug or unexpected behavior in Vorssaint
labels:
  - bug

body:
  - type: markdown
    attributes:
      value: |
        Fill in what you can. A partial report is still useful.

        **One problem per issue.** A second problem added to an existing thread
        tends to get lost while the first one is being discussed.

        [Reporting a useful bug](https://github.com/vorssaint/vorssaint-utils/blob/main/docs/TROUBLESHOOTING.md#reporting-a-useful-bug)
        lists what a report needs to be actionable.

  - type: checkboxes
    id: preflight
    attributes:
      label: Before you file
      description: The [troubleshooting guide](https://github.com/vorssaint/vorssaint-utils/blob/main/docs/TROUBLESHOOTING.md) covers both of these.
      options:
        - label: "If the feature does nothing at all, I checked that Vorssaint is listed and switched on for the permission it needs in System Settings, Privacy and Security, and toggled it off and back on."
          required: true
        - label: "It still happens on the latest version."
          required: true

  - type: textarea
    id: description
    attributes:
      label: Description
      description: A clear and concise description of what the bug is.
      placeholder: Describe the bug... (expected and actual behaviour)
    validations:
      required: true

  - type: textarea
    id: reproduce
    attributes:
      label: Steps to Reproduce
      description: Steps to reproduce the behavior.
      placeholder: |
        1. Open Vorssaint
        2. Click on '...'
        3. See error
    validations:
      required: true

  - type: dropdown
    id: feature_area
    attributes:
      label: Feature Area
      description: The same groups you see in Settings.
      options:
        - Windows and Dock
        - Mouse and keyboard
        - Clipboard and files
        - Sound
        - Energy and display
        - Tools
        - System monitor
        - Menu bar
        - Other
    validations:
      required: true

  - type: textarea
    id: screenshots
    attributes:
      label: Screenshots / Recordings
      description: Add screenshots or recordings if applicable.

  - type: input
    id: vorssaint_version
    attributes:
      label: Vorssaint Version
      description: The number shown in Settings, on the About page.
      placeholder: Copy the number from the About page
    validations:
      required: true

  - type: dropdown
    id: macos_version
    attributes:
      label: macOS Version
      options:
        - macOS 27 (Golden Gate)
        - macOS 26 (Tahoe)
        - macOS 15 (Sequoia)
        - macOS 14 (Sonoma)
    validations:
      required: true

  - type: input
    id: language
    attributes:
      label: Language
      description: The language Vorssaint is running in, and your macOS language if it differs. Several bugs so far were invisible in English, so this narrows things down fast.
      placeholder: "简体中文 · macOS in 简体中文"
    validations:
      required: true

  - type: input
    id: hardware
    attributes:
      label: Mac and displays
      description: Skip only if the bug touches neither windows nor hardware. The Mac model decides the sensors, fans and brightness range; the display and Spaces layout decides almost every window bug.
      placeholder: "MacBook Pro 14 (M3 Pro) · built-in + one 4K external, scaled, two Spaces"

  - type: input
    id: installation_method
    attributes:
      label: Installation Method
      placeholder: Homebrew, DMG, GitHub Release, built from source
